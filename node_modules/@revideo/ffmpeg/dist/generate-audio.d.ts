import { AssetInfo } from '@revideo/core';
export declare function generateAudio({ outputDir, tempDir, assets, startFrame, endFrame, fps, }: {
    outputDir: string;
    tempDir: string;
    assets: AssetInfo[][];
    startFrame: number;
    endFrame: number;
    fps: number;
}): Promise<string[]>;
export declare function mergeMedia(outputFilename: string, outputDir: string, tempDir: string): Promise<void>;
//# sourceMappingURL=generate-audio.d.ts.map